#include <iostream>
#include <string>
#include "parser.hpp"


using namespace std;

vector <Node*> Parser::parse(vector<Token> &tokens){

    token = tokens.begin();
    int time = (int)tokens.size();

}